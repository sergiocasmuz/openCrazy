<form method="post" action="{{ path('planilleros_delete', {'id': planillero.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ planillero.id) }}">
    <button class="btn btn-bitbucket fa fa-trash BTNa"></button>
</form>
