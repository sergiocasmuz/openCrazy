{% extends 'base.html.twig' %}

{% block title %}Viajes{% endblock %}

{% block body %}
    <h1>Viajes</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ viaje.id }}</td>
            </tr>
            <tr>
                <th>Salida</th>
                <td>{{ viaje.salida ? viaje.salida|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Desde</th>
                <td>{{ viaje.desde }}</td>
            </tr>
            <tr>
                <th>Hasta</th>
                <td>{{ viaje.hasta }}</td>
            </tr>
            <tr>
                <th>Llegada</th>
                <td>{{ viaje.llegada ? viaje.llegada|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Timestamp</th>
                <td>{{ viaje.timestamp ? viaje.timestamp|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('viajes_index') }}">back to list</a>

    <a href="{{ path('viajes_edit', {'id': viaje.id}) }}">edit</a>

    {{ include('viajes/_delete_form.html.twig') }}
{% endblock %}
